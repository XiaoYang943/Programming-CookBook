## 4.3.2 KDB 树索引

KDB 树综合了 KD 树和 B 树的特点，是 B 树在多维空间上的扩展形式。它将 KD 树的空间分割思想和 B 树适合外存组织的特点结合起来，既能按空间划分组织多维数据，又能使索引结构适应磁盘页存储。KDB 树的内部结点保存分割信息和子结点指针，叶子结点保存实际数据对象或数据对象的引用。

KDB 树的优点是可以处理多维点数据，并能较好地支持范围查询；它的缺点是插入、删除和结点分裂处理较为复杂，尤其在空间数据分布不均匀时，索引结构的维护代价较高。对于线状、面状对象，需要先采用近似表示或空间映射方式转换成点或区域信息，然后再纳入索引。

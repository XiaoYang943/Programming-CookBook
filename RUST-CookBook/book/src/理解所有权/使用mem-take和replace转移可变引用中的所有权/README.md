# 使用mem::take和mem::replace转移可变引用中的所有权

> 本文整理自 Rust Design Patterns 的 Idiom：[`mem::{take(_), replace(_)}`](https://rust-unofficial.github.io/patterns/idioms/mem-replace.html)。

归入“理解所有权”，因为该惯用法用于在借用规则内安全转移可变引用中字段的所有权。

## 待补充

- `mem::take`
- `mem::replace`
- 配套Demo与实践检查表
